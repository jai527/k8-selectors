apiVersion: v1
kind: pod
metadata:
  name: pod-toleration
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx
  toleration:
  - key: "project"  
    operator: "Equal"
    value: "roboshop"
    effect: "NoSchedule"